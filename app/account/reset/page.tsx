'use client';
import Link from 'next/link';
import { FormEvent, useState } from 'react';
export default function ResetPage(){const [password,setPassword]=useState('');const [message,setMessage]=useState('');async function submit(e:FormEvent){e.preventDefault();const token=new URLSearchParams(location.search).get('token');const response=await fetch('/api/v1/account/reset-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const data=await response.json() as {error?:string};setMessage(response.ok?'Password updated. You can sign in now.':data.error||'Reset failed');}return <main className="grid min-h-screen place-items-center bg-[#d9d3ff] p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8"><h1 className="text-4xl font-black">Reset password</h1><input type="password" minLength={10} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" className="mt-8 w-full rounded-xl border p-3"/><button className="mt-4 w-full rounded-xl bg-black p-3 font-bold text-white">Update password</button>{message&&<p className="mt-4 text-sm">{message}</p>}<Link href="/account" className="mt-5 block text-sm underline">Back to sign in</Link></form></main>}
